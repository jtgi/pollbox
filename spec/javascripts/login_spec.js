define([
    "modules/session"
],

    function(Session){
    describe("Session", function() {
        it("Should display two fields", function() {
            var sessionModel = new Session.Model();
        });

    });
});
